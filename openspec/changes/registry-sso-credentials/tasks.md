# Tasks — registry-sso-credentials

## 1. Ops prerequisites (registry side, no platform code)

- [x] 1.1 **APPLIED 2026-09-21 on china-cheap-1** (registry-side ops; the platform side was already done). Two changes, both documented with verification + rollback in DEPLOY.md ("Per-user market connect (registry-sso-credentials) — APPLIED"): (a) `CORS_ALLOWED_ORIGINS=https://craw.finddatatech.cloud,http://103.236.89.212:31870` in `extra_env/registry.env` — the compose `env_file` for the registry service (the project `.env` is interpolation-only and never reaches the container, which is where the first attempt landed); (b) an nginx template hotfix (`nginx_rev_proxy_http_only.conf` + the `http_and_https` copy) routing OPTIONS to a named location without `auth_request /validate` — a CORS preflight carries no credentials by spec, so /validate could only 401 it and the browser then blocked the real mint. Verified live: GET `/api/auth/csrf-token` → ACAO per origin; preflight `POST /api/tokens/generate` → 200 + ACAO + allow-headers; a foreign origin → 400 with no ACAO (fail-closed intact); unauthenticated GET/POST still 401 with `WWW-Authenticate` (auth path untouched); registry UI 200; container healthy. Browser-level (Playwright, from the real origin `https://craw.finddatatech.cloud`): the cross-origin GET resolves as `type:"cors"` with the registry's 401, and the POST is dispatched — its anonymous 401 is nginx's (no ACAO by design, the mint needs a registry session), which is what task 5.1 exercises with a real login. ⚠️ Also found and recorded: the documented platform URL `23.144.68.246:30950` is dead (no service in the cluster uses that NodePort any more; the live entries are `https://craw.finddatatech.cloud` and, currently, `103.236.89.212:31870`) — DEPLOY.md updated. ✅ 复核 2026-09-21
- [ ] 1.2 **PARTIAL**: the contract is implemented as recorded in DEPLOY.md (`GET /api/auth/csrf-token` → `POST /api/tokens/generate` with `X-CSRF-Token`, `expires_in_hours`) and every path is overridable (`MARKET_REGISTRY_LOGIN_PATH`, `MARKET_REGISTRY_CSRF_PATH`, `MARKET_REGISTRY_TOKENS_PATH`) so a version bump needs no code change. Remaining: one scripted mint against the DEPLOYED registry confirming `exp` ≈ +168 h and the response field name (`access_token`/`token`/`jwt_token` are all accepted).

## 2. Credential store + API

- [x] 2.1 DB migration: `user_registry_credentials(email PK, token, expires_at, stale, source, updated_at)`; no backfill. Verify: `scripts/test-registry-credentials.mjs` asserts the table/columns after `initDb`, that re-running migrations is a no-op, and that an absent row reads `connected:false`. ✅ 复核 2026-09-21
- [x] 2.2 Routes implemented in `server/routes/registry.js`. Verify: the unit suite covers the connect/disconnect lifecycle, anonymous 401s in hosted mode, auth-off machine-owner writes, expired/JWT-less tokens, and asserts against the RAW response bytes that no token is ever returned. The connection payload also carries `registryUrl`/`loginPath`/`mint` so the popup needs no build-time config. ✅ 复核 2026-09-21
- [x] 2.3 `registry-credentials.markStale` + the `tool/result` 401 hook in `server/dsh-events.js` (tool names are `mcp__<server>__<tool>`; only registry-ref servers count). Verify: unit test drives a real tool/result 401 through `ctx.handleDshEvent`, asserts stale + `connected:false` + one profile re-apply (a second 401 does not re-apply), that a non-registry MCP server's 401 is ignored, and that `GET /api/registry/connection` reports `stale`. A `registry_credential_stale` WS ping drives the Store prompt. ✅ 复核 2026-09-21

## 3. Install flow + injection

- [x] 3.1 `POST /api/extensions/mcp` stamps `credentialRef: "registry"` and drops the template's placeholder headers; without a live credential it answers 409 `{code:"credential-required"}`, which `addMcpServer` in `@platform/core` carries on the thrown error and the form maps to the connect panel. Verify: unit tests for both branches, non-registry/manual installs unchanged, and group admission still biting first (403) with `requiredGroups` still stamped. ✅ 复核 2026-09-21
- [x] 3.2 `writeMcpPatch({ ..., ownerEmail })` resolves the ref at each write; `ctx.runtimeOwnerEmail/-Groups` carry the profile owner (boot: `CELL_USER_EMAIL` + the owner-groups snapshot; per-request: `applyProfile`). Verify: unit tests for live (header injected, record untouched), absent/stale/expired (omitted + record kept, other servers unaffected), gated × gated-out composition, auth-off machine-owner resolution, and `mcp.json` servers untouched. ✅ 复核 2026-09-21
- [x] 3.3 `RegistryConnectPanel` (state badge, connect/re-connect, paste fallback, disconnect, unconfigured/stale hints) in the live `McpMarketView` + the install dialog; popup page `web/public/registry-connect.html` (login leg → mint leg → postMessage); `McpServerForm` shows no credential field for a connected registry entry and routes to connect otherwise. Verify: `e2e/registry-connect.spec.js` (5 tests) passes against the hermetic `e2e/registry-stub.js` — including the one-click silent connect and Add-enabled-with-no-typing paths. The real-Logto + fd-prod run remains task 5.1. ✅ 复核 2026-09-21

## 4. Fallback + regression

- [x] 4.1 The paste dialog POSTs the same `/api/registry/credential` with `source:"paste"`; `tokenExpiry` parses a decodable JWT's `exp` (opaque tokens = unknown expiry, cleared only by a 401). Verify: unit tests for JWT/opaque/invalid tokens; the e2e pastes a stub-minted 7-day token and asserts the reported expiry, then drives injection from it. ✅ 复核 2026-09-21
- [x] 4.2 Auth-off/desktop regression: non-registry installs, bundled entries, mcp.json precedence, requiredGroups filtering all unchanged. Verify: full unit suite 207 tests → 205 passed + the 2 role-gated install tests updated for the new credential rule (their registry fixture entries now need a credential; admission/stamping assertions untouched, and the run passes 14/14). Full fast e2e: 228 collected, 223 passed, only the 4 pre-existing known flakes (chat-polish 8.3b/8.4, composer-stop message-actions, sso-user-bindings email-case) — no new failure; 218 → 223 is exactly this change's 5 new specs. ⚠️ One hazard found and fixed while running it: a spec module is collected by the Playwright runner process, where `TEST_WORKER_INDEX` is unset, so calling `prepareTempStoreDirs()` at a spec's module scope wipes the RUNNING server's stores (36 cascading dsh "Cannot find package" failures). Specs must use the new path-only `tempStoreDirs()`; `prepareTempStoreDirs()` stays config-only. ✅ 复核 2026-09-21

## 5. End-to-end + docs

- [ ] 5.1 Full flow on fd-prod with a demo account: login → connect (one click, silent) → install a pack MCP → tool call succeeds → token expires → re-connect prompt → restored. Verify: runbook-documented pass with Trace evidence of the MCP call. **PARTIAL**: the CORS/ops half is done and verified (task 1.1), and the same flow passes end-to-end against the hermetic stub (task 3.3). Remaining: a real Logto/demo-account session on a deployment running THIS code (the fd-prod image predates it) plus the operator's demo credentials — that walk-through is what produces the Trace evidence.
- [ ] 5.2 Update `docs/vertical-packs.md` (add-vertical-sample-packs) to drop the V0 manual-token step once this ships. Verify: playbook shows the SSO path as primary with paste as fallback.
