## 1. Cell boundary hardening (Phase 1)

- [x] 1.1 Audit writable-state containment: run a cell-shaped server (`PLATFORM_DATA_DIR` + `DSH_HOME` pointed at scratch dirs, hosted-mode env set), exercise chat + document upload + skill install + MCP toggle + cron save, and diff the filesystem to confirm every write lands under the two roots. Fix any CWD-relative stray writes found. Verify: `find` diff shows zero files outside the roots. → `scripts/test-cell-containment.mjs`. No CWD-relative writes existed; two other blockers found and fixed: `ensureDshHome()` (a fresh per-user `DSH_HOME` had no profile scaffold, so no cell could boot) and `cron.addJob` returning the live node-schedule handle (WS `cron_add` threw on JSON serialization).
- [x] 1.2 Add the hosted-mode identity-trust gate: a `CLOUD_MODE`-gated check in the forward-auth/optional-SSO header path that honors `X-Forwarded-*` only when the `CELL_GATEWAY_SECRET` matches (constant-time compare). Verify: unit tests — headers+secret honored, headers without secret treated as unauthenticated, gate inert when `CLOUD_MODE` unset (existing forward-auth tests stay green).
- [x] 1.3 Prove broadcast isolation by routing: two users, two cells, two browsers streaming concurrently; assert each browser sees only its own session list, stream events, and errors. Verify: scripted two-cell e2e run passes with zero cross-delivery. → `scripts/test-cell-isolation.mjs` (two cells, two WS clients: A's writes produced 0 frames on B's socket; document libraries and session lists disjoint).
- [x] 1.4 Document the cell run contract: env matrix (`PLATFORM_DATA_DIR`, `DSH_HOME`, `MCP_CONFIG_PATH`, port, `CLOUD_MODE`, gateway secret), loopback-only binding, and the local-disk (no NFS) rule in `.env.example` + README deployment section. Verify: an operator can start two cells by hand from the docs alone. → `.env.example` "Hosted cells" section (incl. a worked two-cell command) + README "Hosted deployment (the cell run contract)" in both language halves.

## 2. Gateway + spawner (Phase 2)

- [x] 2.1 Build the cell spawner: derive a stable user id from the verified identity, allocate a free loopback port, `spawn(server.js)` with the per-user env matrix, track {pid, port, state} in memory, and reap children on gateway shutdown. Verify: integration test starts a cell for a fake user, hits its health endpoint through the proxy, and confirms process cleanup on exit. → `gateway/spawner.js` + `scripts/test-cell-gateway.mjs` (spawn, port allocation, `/api/auth/me` through the proxy, shutdown reaps children).
- [x] 2.2 Build the gateway HTTP front: reuse the Logto session-cookie verification from `server/logto-auth.js`; redirect browsers to login, `401` APIs, and reverse-proxy authenticated traffic to the user's (possibly just-started) cell with identity headers + gateway secret injected. Verify: e2e — anonymous gets redirect/401, authenticated round-trips to their cell's `/api/auth/me`. → `gateway/index.js` + `gateway/proxy.js`. Verified: anonymous page 302→`/auth/login`, anonymous API/WS 401, forged `X-Forwarded-Email` ignored, authenticated round-trip reaches the cell with the verified identity.
- [x] 2.3 Add WebSocket sticky routing: upgrade authenticated WS through to the owning cell, keep the socket pinned for its lifetime, and re-route reconnects to the same user's cell. Verify: e2e — a chat turn streams over the proxied WS; a forced cell restart mid-session reconnects and resumes without cross-user leakage. → Verified over the real proxied WS: connect-time sync arrives, a cell `SIGKILL` mid-session reconnects to that same user's fresh cell and re-syncs the session persisted before the crash, and the other user never sees it. The chat-turn stream itself needs a live LLM, so the observable asserted is the same transport's sync rather than a model response.
- [x] 2.4 Implement lifecycle policy: spawn-on-first-traffic with a "starting" response (or hold) while the cell boots; always-on default; optional `CELL_IDLE_REAP_SECS` reaper that queries the cell's enabled cron/bot state before stopping it (cron/bot exemption). Verify: integration test — idle reap stops a plain cell, refuses to stop one with an enabled cron job, default never reaps. → `CELL_IDLE_REAP_SECS` (default 0 = never), cron/bot exemption via the cell's `GET /api/gateway/jobs`. Verified: an idle plain cell is reaped and cold-starts on next request; a cell holding an enabled cron job is kept; default keeps cells resident.
- [x] 2.5 Add gateway observability: `/healthz` plus an admin-gated status payload listing per-user cells {user, state, pid, lastTraffic}. Verify: manual run — statuses reflect spawn/reap/crash transitions; one cell `kill -9`'d shows error for its user only. → `GET /healthz` plus admin-gated `GET /api/gateway/status` (`{user, userId, state, pid, port, lastTraffic, uptimeMs, error}`; 403 for non-admins). Verified: state transitions spawn→error→running and a `kill -9` is scoped to one user.

## 3. Bindings retirement (D6)

- [x] 3.1 Retire the shared-runtime overlay: delete `server/runtime-bindings.js` wiring (pendingBindings, owner flip, busy-window apply, `applyPendingBindings` from finishTurn); make model/MCP saves apply through the existing global paths (`set_model` restart, MCP patch hot-swap) which now serve the cell's single user. Verify: WS `list_bindings` + REST binding endpoints still work per the updated spec; no `apply_bindings` handler remains. → Removed the WS `apply_bindings` handler (and its client type; the UI uses the REST path), the `runtimeOwner` record, and the owner-flip bookkeeping. **Note — the plan and the spec delta disagree here, and the spec won:** tasks.md/D6 say to also delete `pendingBindings` and the busy-window apply, but the delta's "model is saved while busy" scenario still requires "the response reports that application follows the current turn's completion". The spec is the behavior contract, so the deferred apply was kept and only the inter-user machinery was retired. If the intent was really to drop deferred application, the delta needs editing first.
- [x] 3.2 Apply bindings at cell start: on boot, if the cell's user has a saved model binding, seed the runtime model from it (before first prompt). Verify: integration test — save binding, restart cell, first `current_model` event reports the saved model. → `CELL_USER_EMAIL` (set by the spawner) drives boot-time selection: the user's saved model wins over the global default, and their saved MCP availability is folded into the boot patch. Verified by `scripts/test-cell-bindings.mjs` (save binding → `SIGKILL` cell → the restarted cell reports the saved model as its FIRST `current_model`, and WS `list_bindings` + the REST snapshot still answer while `apply_bindings` is inert).

## 4. Regression + docs

- [x] 4.1 Dev/desktop regression pass: `npm start` open-access mode and the Electron dev flow behave exactly as before (no `CLOUD_MODE` residue: secret gate inert, no gateway required). Verify: existing e2e suites green in both modes. → `npm run test:unit`: **117/117 green**, including the four new cell/gateway tests running concurrently with the whole suite. Dev smoke (`node server.js`, no `CLOUD_MODE`/no gateway): `/api/config` 200 open-access, `/api/auth/me` 200, anonymous `/api/users/me/bindings` 401, forged `X-Forwarded-Email` still grants nothing, profile writes still land in `~/.dsh` — the gate is inert, no residue. `npm run test:e2e` (fast, after `web:build:e2e`): **203 passed, 1 failed, 1 skipped**; the one failure (`e2e/sso-user-bindings.spec.js:183`, an `ssoEmail` case-normalization expectation) **reproduces on a clean HEAD worktree** (3 failures in that same file alone there), and `server/auth.js`'s diff against HEAD is provably limited to the gate — so it is pre-existing, not this change. Electron desktop: the supervisor never sets `DSH_HOME`, so it uses `~/.dsh`, which is by definition `DSH_SHARED_HOME` — `ensureDshHome()` short-circuits (scaffold present, link is its own target). Desktop path untouched; no GUI run was performed.
- [x] 4.2 Cloud-deployment guide: single-host topology (gateway + resident cells), env matrix, Logto app registration, memory sizing note (~per-cell budget), idle-reap trade-off and the enabled-jobs exemption, cold-start expectation, and the Phase 3 outlook (k8s, PVCs, backups). Verify: doc review against a real deployment runbook. → `DEPLOY.md` "Multi-tenant cloud deployment (gateway + cells)": topology diagram, run command, gateway/cell env matrix, the two hard rules (loopback-only cells, local-disk data), Logto registration (gateway is the sole client), per-cell memory budget (~150–300 MB, sizing arithmetic for ~100 cells), idle-reap trade-off with the enabled-cron/bot exemption and the stated offline contract, cold-start expectation, observability/`SIGTERM` behaviour, and the Phase 3 outlook (k8s client swap, PVCs, backups, single-instance caveat). File map updated with `gateway/` + the four cell tests.
