# migrate-pi-to-dsh

Replace @earendil-works/pi-coding-agent with DeepSeek Harness (dsh) via subprocess JSON-RPC bridge (Shape B), refactoring current custom code to dsh plugins where dsh already provides equivalents
