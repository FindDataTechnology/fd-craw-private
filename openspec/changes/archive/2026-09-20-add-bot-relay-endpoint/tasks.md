# Tasks — add-bot-relay-endpoint

## 1. Schema and storage

- [x] 1.1 Add migration v14 creating `bot_chats(bot_id, chat_key, sender_name, first_seen_at, last_seen_at)` with primary key `(bot_id, chat_key)`, `bot_channels(name, bot_id, chat_key, created_at)` with primary key `name`, and `bot_relay_log(ts, channel, bot_id, text_chars, outcome, error)`. Verify: a fresh boot applies v14 and a second boot is a no-op, and an existing DB keeps its `bots` and `chat_sessions` rows (assert row counts before/after with `sqlite3 data/app.db`, and `npm run test:unit` stays green including `scripts/test-persistence.mjs`). _(Done. Verified against a `.backup` copy of the real dev DB rather than the live file: boot 1 logged `applied migration v14`, boot 2 logged nothing and stayed at schema v14, 62 `chat_sessions` rows and 0 `bots` rows unchanged across both boots. `npm run test:unit` green: 186/186. Also verified on the running server, where the relay log row landed in the real schema.)_
- [x] 1.2 Add the DB accessors: `upsertBotChat`, `listBotChats`, `getChannel`, `listChannels`, `createChannel`, `deleteChannel`, `insertRelayLog`. Verify with a new `scripts/test-bot-relay.mjs` in the `node --test scripts/test-*.mjs` suite covering upsert-idempotence (same chat twice yields one row with an advanced `last_seen_at`) and channel-name uniqueness. _(Done: `db.js` also carries `getBotChat` (the binding precondition) and `listRelayLog` (makes the audit readable, used by the tests). Idempotence asserted in `scripts/test-bots.mjs` ("records its chat for later addressing"); uniqueness via the 409 on a duplicate name in `scripts/test-bot-relay.mjs`.)_

## 2. Inbound seen-chat recording

- [x] 2.1 Record the chat in the verified inbound path (`server/bots.js`, at the point the message is accepted and before the turn runs), upserting `(bot_id, chat_key, sender_name)` and isolating failures — a throw is logged, never propagated. Verify: extend `scripts/test-bots.mjs` to assert a first message creates the row, a second updates it without duplicating, a forced store failure still produces the agent reply, and no table column holds message text. _(Done: `recordChat()` sits after the size check and before the rate limiter, wrapped in try/catch. Three tests added; the forced-failure one swaps `ctx.db` for a throwing stand-in and asserts the reply still went out plus the warning was logged. The text-marker assertion covers "no column holds message text"; an oversized message is deliberately not recorded.)_

## 3. Relay endpoint

- [x] 3.1 Add `/api/bots/relay/` to `AUTH_EXEMPT_PREFIXES` in `server/auth.js` and expose the constant-time compare helper for reuse (the `secretMatches` logic). Verify: extend `scripts/test-auth-mode.mjs` to assert the relay path is reachable without identity headers while `/api/bots` CRUD remains identity-gated in both `forward_auth` and `logto` modes. _(Done: `secretMatches` exported, prefix added with a comment naming the route and its token. New test asserts POST to the prefix reaches the handler with no identity in both modes while `/api/private` 401s; confirmed on the running server too (200 on `/api/bots/chats` under `AUTH_MODE=none`, i.e. the admin routes still sit behind the normal gate).)_
- [x] 3.2 Implement `POST /api/bots/relay/send` as its own route module: verify the bearer token before touching the body, answer 401 for a missing/incorrect token without logging the payload, and answer 404 (inert) when `BOTS_RELAY_TOKEN` is unset. Verify with `scripts/test-bot-relay.mjs`: 401/404 matrix, and a body sent with a bad token appears in no log output. _(Done: `server/routes/bot-relay.js`, registered before the bots routes in `server.js` (otherwise `/api/bots/:id/send` would swallow the path with `id = "relay"`), and `server.js` skips its global JSON parser for the prefix. Auth is a middleware mounted ahead of the route's own parser — a real bug this task caught: with the parser first, a malformed body with a bad token answered 400 instead of 401. Verified on the running server: `{oops` + wrong token → 401. Also added: a body-parser failure with a valid token answers 400/413 instead of Express's default 500.)_
- [x] 3.3 Resolve the channel and deliver: unknown channel → 404 with no platform call; a channel whose bot is disabled → refused; caller-supplied `botId`/`chatKey` fields have no effect on the destination. Verify in `scripts/test-bot-relay.mjs` with a stubbed adapter asserting the exact `(credentials, chatKey, text)` triple reaching `sendText` for each case. _(Done: the stub captures the full call unprojected; the delivery test asserts `{cred: {token}, chatKey, text}` exactly. Disabled bot → 409, deliberately stricter than the admin endpoint, whose pass-through behavior is asserted unchanged in the same test. One endpoint-to-endpoint test drives a real webhook → recorded chat → binding → relay send.)_
- [x] 3.4 Enforce the guards and write the audit row: per-channel limit of 10 sends/min and a 4000-character cap as named constants beside `RATE_LIMIT_PER_MIN` / `MAX_MESSAGE_CHARS`, plus one `bot_relay_log` row per attempt (channel, bot, outcome, `text_chars`, error) that never contains the text. Verify in `scripts/test-bot-relay.mjs`: the 11th send in a minute is 429 with no platform call, an oversize body is rejected, and the recorded row for a successful send carries the length but not the text. _(Done: `MAX_RELAY_CHARS` (exported for the route's message), `RELAY_RATE_PER_MIN`, `RELAY_SEND_TIMEOUT_MS`; the inbound bucket was extracted into a shared `takeBucket()` with identical refill semantics. Beyond the task: platform errors are redacted of credential values before reaching the caller or the audit row (Telegram's URL carries the bot token), and a 15 s race bounds the caller's wait. Audit confirmed on the running server: `ops-alerts|rejected|2|unknown channel`.)_

## 4. Admin channel management

- [x] 4.1 Add admin-gated REST routes to list, create, and delete channel bindings, creating only from recorded `bot_chats` pairs and returning no bot credentials. Verify: a script/e2e check that create with an unrecorded pair is 400, create from a recorded pair succeeds, list exposes name/bot/chat key/created time only, the routes are 403 without the admin group, and a deleted channel immediately answers 404 on the relay. _(Done in `server/routes/bots.js`: `GET /api/bots/chats` (+`botName`), `GET|POST /api/bots/channels`, `DELETE /api/bots/channels/:name`, channel names `[a-z0-9][a-z0-9._-]{0,63}`. All five assertions covered in `scripts/test-bot-relay.mjs`; the 403 case runs against a second app in `forward_auth` mode. Verified on the running server: binding an unrecorded chat answered "That chat has never messaged this bot".)_

## 5. Deployment on fd-prod

- [ ] 5.1 Deploy with `BOTS_RELAY_TOKEN` unset first and confirm the route is inert on the live service (`curl -i http://23.144.68.246:30950/api/bots/relay/send` → 404). Then add the token: `kubectl --context cheap -n fd-prod patch secret platform-secrets -p '{"stringData":{"BOTS_RELAY_TOKEN":"<token>"}}'`, `rollout restart deploy/platform`, `rollout status`. Verify: wrong token → 401, correct token → a channel-resolution response (404 for an unknown channel), and the route still works after a pod restart. _(Not done — needs cluster access. `23.144.68.246:30950` is unreachable from this machine (curl timed out), consistent with the DEPLOY.md note that this host has no route to it. The equivalent checks were run against a real local `server.js`: inert 404 with no token and with an arbitrary token, 401 for a wrong token, `{"error":"Unknown channel"}` for a valid one. DEPLOY.md now carries the step-by-step, including the not-over-plain-HTTP-over-the-internet warning.)_
- [ ] 5.2 Verify `PUBLIC_BASE_URL` is set on fd-prod so webhook platforms can receive inbound, which is what populates `bot_chats`. Verify: send one message to a configured bot from the target chat and confirm a `bot_chats` row appears on the deployment (`kubectl --context cheap -n fd-prod exec deploy/platform -- sqlite3 <db> "select * from bot_chats"`), not just in Telegram polling mode. _(Not done — ops. Note the dev DB has 0 bots, so the bots are configured on the deployment; whether `PUBLIC_BASE_URL` is set there is still unverified.)_
- [ ] 5.3 Create a channel binding for that chat via the admin REST route, then perform a live end-to-end send from the cloud caller (the registry MCP's tool path) to the bound channel. Verify: the message arrives in the target chat, a `bot_relay_log` row records the attempt, and a second send to an unbound channel name is refused on the live service. _(Not done — depends on 5.1/5.2, and the caller-side wiring (the cloud MCP wrapping the endpoint as its tool) is outside this repo.)_

## 6. Regression and containment

- [x] 6.1 Run `npm run test:unit` and the live suite (`make test-live`, read-only, against `http://23.144.68.246:30950`) and confirm no regression in bot webhooks, the web chat, or the existing `POST /api/bots/:id/send` behavior (including that a disabled bot is still reachable there, unchanged by D7). _(Unit half done: 186/186 green, with the bots suite (9 existing + 3 new), the auth-mode suite (7 + 1 new) and the new relay suite (14) all passing. Live half not run: the service is unreachable from this machine. Instead, the real `server.js` was booted twice locally against temp data dirs — `AUTH_MODE=none`, `PUBLIC_BASE_URL` set — and `/api/bots`, `/api/bots/chats` and the unshadowed `POST /api/bots/:id/send` (502 for an unknown bot, as before) all behaved; both instances were stopped afterwards.)_
- [x] 6.2 Confirm single-owner containment: a relay send must not add a polling loop or a second platform token cache — verify the boot log still reports one poller per polling bot, and that relay sends while a bot is polling produce no `getUpdates` conflict (no 409 in logs) and no duplicate inbound consumption. _(Code-side done: the relay delivers through the runtime entry's adapter and never calls `initBots`/`reload`, so the process keeps one token cache per platform and one poller. A test instruments the telegram registry's `start` and asserts it is called exactly once per bot and not again by a relay send. The live-log half ("no 409") belongs to 5.3 once a polling deployment runs the relay.)_
